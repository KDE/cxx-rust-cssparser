use std::env;
use std::fs;

// use cxx_rust_cssparser::parser::Parser;

fn main() {
    // let args: Vec<String> = env::args().collect();
    // let path = &args.get(1).expect("A file path is required!");
    //
    // let data = fs::read_to_string(path).expect("Could not read file!");
    //
    // let result = Parser::parse(&data);
    //
    // for rule in result {
    //     println!("{:#?}", rule.selectors);
    //     println!("{:#?}", rule.properties);
    // }
}
